# Ảnh AI — Gemini (Nano Banana). Xem docs/ai-image-prompts.md.
#
# Lấy khoá tại https://aistudio.google.com/apikey và BẬT BILLING cho project của khoá.
# Google đã bỏ gói free cho sinh ảnh, nên khoá chưa bật billing sẽ báo quota bằng 0.
# Sinh chữ vẫn chạy trên gói free — chỉ ảnh mới cần trả phí.
GEMINI_API_KEY=

# Tuỳ chọn, cho tools/fetch-photos.mjs (ảnh thật, không phải ảnh AI)
PEXELS_API_KEY=
UNSPLASH_ACCESS_KEY=
